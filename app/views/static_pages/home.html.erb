<% provide(:title, "ホーム") %>
<div class="center jumbotron">
  <div id="app"><h1>{{message}}</h1></div>
  <p>日常に（お）笑いを</p>
</div>
<% if user_signed_in?%>
  <% if current_user.admin?%>
      <%= link_to "管理者用ユーザー一覧へ", admin_index_path %>
  <%end%>
<%end%> 
<div class="shutty" >
  
    <h3>芸人｜新着順</h3>
    <ol class="talent_users">
      <%= render @users %>
      <%= link_to "芸人一覧へ", users_path, class: "moreToShow" %>
    </ol>
  
</div>


      <script>
        new Vue({
          el: "#app",
          data: {
            message: "お笑い芸人依頼サービス",
          }
          })
      </script>